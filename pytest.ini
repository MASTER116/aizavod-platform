[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no LLM, no DB, free)
    integration: Integration tests (mock LLM, fast)
    e2e: End-to-end tests (real LLM, costs money)
    adversarial: Security/red-team tests
    ab: A/B prompt tests
